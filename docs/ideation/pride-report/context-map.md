# Context Map: Pride Report Fixes

**Phase**: 5
**Gates**: 5/5 ready
**Verdict**: GO

## Gates (Phase 5)

| Gate                 | Status    | Evidence |
| -------------------- | --------- | -------- |
| Scope clarity        | ready     | Every file to touch is named and verified live: `workflows/wave-planner.test.mjs` drift suite at :280-338 (failure assert :320-324; spec's "extract() at :260" is stale — `extract` is :295-301), `package.json` scripts block (:46-54), `README.md` Example section :239-267 with the "Prefer to read the artifacts?" paragraph at :266 (the archive link's home), `.gitignore` `docs/ideation/*` at :22, new `scripts/lint-prompts.mjs` + `scripts/lint-prompts.test.mjs` (auto-globbed by the existing `test` script), evals (two spec-sanctioned locations), and the `docs/ideation/ux-dejank/` archive (12 files present on disk, currently ignored). The one TBD — component 4's (a)/(b) — is pre-decided by the spec ("start with (a)"), and the site line has two named candidate pages with a verified anchor (`walkthrough.astro:44`). |
| Pattern familiarity  | ready     | `scripts/lint-pr-title.mjs` read in full — the exact register for lint-prompts (WHY header, derived-not-duplicated rules, exported pure fns + CLI guard, zero deps); the drift suite's `extract()`/`normalize()` read; `.gitignore`'s own comment (:17-21) documents the re-include mechanics; `test-fixtures/orchestration/` + `open-questions/` establish the fixture convention; every path/anchor reference shape in the prompt corpus enumerated from live grep (see Key Patterns). |
| Dependency awareness | ready     | Blast radius is tiny and fully mapped (Dependencies below): the assert-message change is self-contained, package.json scripts are additive, the README paragraph has no consumers, the un-ignore affects git tracking only, the lint reads but never writes. `execute-contract.mjs` is touched only transiently by component 1's paste-experiment, guarded by the drift suite + vm smoke harness + the 162-test full run. |
| Edge case coverage   | ready     | Concrete list in Edge Cases (Phase 5): per-directory bare-path base resolution (component 2's hard rule), three distinct anchor shapes (`§ N.N`, `core rule N`, italic `*Title*`), `extract()` already strips `export ` making output paste-ready, archive reds are 3 not the spec's 2 (one is stale-CONTENT from Phase 2's deliberate dedupe, not stale-path), `docs/ideation/*` matches direct children only, pride-report/ must stay uncommitted, asciinema present but tape/vhs absent. |
| Test strategy        | ready     | All commands verified live this session: `npm test` baseline = 162 pass / 0 fail / 756ms; `node --test workflows/wave-planner.test.mjs`; `node scripts/lint-prompts.mjs` playground over a 17-file corpus (the <2s wire-in budget is trivially safe); `npm run build` (site deps present); `node scripts/verify.mjs ./docs/ideation/pride-report/contract-data.json` runs today (pass=18 fail=0 judgment=7, commits=4/5) for criteria 22-25; `node scripts/verify.mjs docs/ideation/ux-dejank/contract-data.json` → commits=4/4 pass=12 fail=3 judgment=1. |

## Key Patterns (Phase 5)

- `scripts/lint-pr-title.mjs` — **the script register for lint-prompts.mjs**: shebang + header comment stating WHY the check guards something real and why its rules are *derived, not duplicated* ("The accepted types are READ FROM release-please-config.json rather than duplicated here" :13-16); `ROOT` via `dirname(new URL(import.meta.url).pathname)`; exported pure functions + `process.argv[1] && import.meta.url === \`file://${process.argv[1]}\`` CLI guard (:96) so the test imports without running; **zero dependencies** (hand-rolled parsing — the frontmatter check should follow suit, no yaml package); failure output names the mistake and its consequence. The spec's Failure Mode ("patterns derive from the corpus, not from a copy of it") is exactly this file's config-derivation move.
- `workflows/wave-planner.test.mjs:295-301` — **`extract()`**: from `^(?:export )?function ${name}\(` to `\n}\n`, then `.replace(/^export /, '')` — the emitted replacement text is **already paste-ready** for execute-contract.mjs. `normalize()` (:303-309) strips comments/whitespace for the comparison. Component 1 changes ONLY the assert message at :320-324 (currently `"${name} has drifted from wave-planner.mjs — copy it back verbatim"`) to include `source` formatted for splicing. MIRRORED (:286-291) = detectCycle, computeWaves, propagateSkips, splitWavesByFileOverlap — one `it` per function (:318), so one failure emits one function's text. KEEP IN SYNC markers: `wave-planner.mjs:10`, `execute-contract.mjs:65`.
- **Corpus reference shapes** (component 2's check surface, all verified live by grep):
  - `${CLAUDE_PLUGIN_ROOT}/{references,workflows,scripts,agents,skills}/...` — resolves from repo root; ~46 occurrences across 8 files (heaviest: ideation/SKILL.md 17, autopilot 8, execute-spec 8).
  - Bare `references/foo.md` inside `skills/ideation/SKILL.md` — resolves to `skills/ideation/references/`, **explicitly declared at :307** ("bare `references/...` mentions in this file resolve there"). This is the per-directory base rule a naive linter gets wrong.
  - Bare sibling filenames among shared refs — `interview-engine.md core rule 1` (confidence-rubric.md:9, :138) — same-dir relative.
  - `§ N[.N]` → numbered headings: harness-compat `## 2.` (:20) / `## 3.` (:36); ideation `### 5.4 Present Handoff Summary` (:234). Both cross-file ("ideation SKILL.md § 5.4", get-goal-prompt:22) and intra-file (ideation:118).
  - `core rule N` → numbered **list items** under `### Core rules` (interview-engine.md:100-105, rules 1-4) — NOT headings; a distinct pattern from §.
  - Italic anchors `*What to explore*` → `### What to explore` (interview-engine.md:106); `*When to stop*` → `### When to stop` (:213). Referenced from feedback-loop-guide.md:64 and brainstorm/SKILL.md:23.
  - Out of v1 scope per "machine-shaped only": bare numeric refs without § (express/SKILL.md:122 "4.2's phasing", :128 "(5.4)") — flag in phase notes, don't check.
- `.gitignore:17-23` — **the re-include mechanics, documented in-repo**: `docs/ideation/*` (not `docs/ideation/` — the comment explains git cannot re-include a file under an ignored directory) with the `!docs/ideation/learnings.md` precedent one line below. `*` doesn't cross `/`, so `!docs/ideation/ux-dejank/` re-includes the dir and its contents; verify with `git check-ignore -v docs/ideation/ux-dejank/contract.md` (expect no match, exit 1) + `git status --porcelain docs/ideation/ux-dejank/` (expect 12 `??` entries).
- `test-fixtures/orchestration/` and `test-fixtures/open-questions/` — **the fixture convention**: directory with `README.md` + `contract-data.json` (+ specs, + a test where mechanical). Eval transcripts land here (tracked) or under `docs/ideation/pride-report/evals/` (auto-ignored via `docs/ideation/*`) — the spec sanctions both; no transcript precedent exists anywhere in the repo (the word appears only in prose).
- `README.md:266` — **the "Prefer to read the artifacts?" paragraph** (Phase 4's addition): the one-line archive link ("a real run, unedited") joins this paragraph alongside the test-fixtures and examples links.
- `site/src/pages/walkthrough.astro:44` — "Every transcript below is the shape of a real run" — the natural site anchor for the one-line archive link (guide.astro is the alternative; both were touched in Phase 4).
- **SKILL.md frontmatter shape**: all 5 SKILL.md carry `name` + `description` in the first `---` fence; some add `allowed-tools` (YAML list) and `disable-model-invocation`. Hand-parse: first fence, `^name:`/`^description:` presence — no parser needed.

## Dependencies (Phase 5)

- `workflows/wave-planner.test.mjs` — consumed by → `npm test` glob (`workflows/*.test.mjs`) only. The assert-message change is self-contained; the paste-experiment transiently edits `execute-contract.mjs`, guarded by this same suite + `execute-contract.smoke.test.mjs` (vm harness) + the full 162-test run.
- `package.json` scripts (:46-54) — consumed by → developers + CI (`node --run test` per the .gitignore comment block). `lint:prompts` is additive; wiring into `test` is conditional on <2s (corpus = 17 md files under skills/+references/, optionally agents/*.md + workflows/README.md — builder's scope call, see Edge Cases).
- `scripts/lint-prompts.mjs` (new) — consumed by → the `lint:prompts` script and `scripts/lint-prompts.test.mjs` (imports pure functions per the lint-pr-title register). Its own test asserts zero violations on the live corpus — Phase 2's single-owner pass is what makes that achievable; the "Phase 2.2" dangling reference that motivated this component was fixed at feedback-loop-guide.md:64 in Phase 2.
- `README.md:266` — no consumers; prose only.
- `.gitignore` — consumed by → git tracking. The un-ignore makes the 12 ux-dejank files trackable; nothing else under `docs/ideation/` changes state (current contents: `learnings.md` tracked, `pride-report/` + `ux-dejank/` ignored).
- `docs/ideation/ux-dejank/` — consumed by → nothing tracked today (fully ignored; verified: 12 files — context-map.md, contract-data.json, contract.html, contract.md, 4 implementation-notes, 4 specs). After commit: README Example link + one site line. Its contract-data.json stays verifiable: `node scripts/verify.mjs docs/ideation/ux-dejank/contract-data.json` → commits=4/4 pass=12 **fail=3** judgment=1 — the in-archive README must explain all three reds accurately (see Edge Cases).
- No other external consumers — the four components share no files and are independently committable, matching the spec's per-component rollback plan.

## Conventions (Phase 5 additions; Phase 1-4 conventions still apply)

- **Script header comments**: state WHY the check guards something real (the incident it prevents), then why the rules are derived rather than duplicated (lint-pr-title.mjs:3-16). lint-prompts' header should name the dangling-"Phase 2.2" incident class and the derive-from-corpus rule.
- **CLI/test duality**: export pure functions; guard the CLI with the `process.argv[1]`/`import.meta.url` comparison so tests import without side effects (lint-pr-title.mjs:96).
- **Failure output register**: one line per violation — file, line, broken reference — naming the mistake and the consequence, not restating grammar.
- **No new dependencies**: repo scripts are dependency-free Node; hand-parse frontmatter rather than adding a yaml package.
- **Test conventions** (Phase 1, binding): `node --test`, `node:assert/strict`, `<module>.test.mjs` beside the module — auto-globbed by package.json's `test`.
- **Archive framing** (spec Failure Modes): dated history, never current docs — the in-archive README states the run date and explains its reds before anything else.
- **Decision recording**: this phase is explicitly cut-happy ("cut toward the smaller version and record the decision") — rejected larger versions (engine inliner, automated eval harness, terminal capture) go to the phase's decision record / Open Items, not silent omission.

## Edge Cases (Phase 5, for the builder)

- **Spec premise drift on the archive reds**: the spec says "2 red verify criteria on deleted paths" — reality is **3 reds** (verified live): [9] learning-capture grep (`accept/edit/dismiss|up to 3` in both execution skills) fails on stale-CONTENT, not stale-path — Phase 2 deliberately deduped those sections to pointers at learning-filter.md; [14] pins `"version": "0.19.0"` in plugin.json (now 0.23.0 — its test paths still exist); [15] `docs/workflow-example.html` is a genuinely deleted path. The in-archive README must explain all three accurately — [9] is "the product improved past the criterion," the learning loop's own best lesson. Copying the spec's "2 stale-path reds" framing verbatim would be wrong twice.
- **`extract()` output is paste-ready but partial**: it strips `export ` (:301) and captures only the function body — the paste target in execute-contract.mjs sits under the KEEP IN SYNC header (:65) with per-function comments; normalize() ignores comments, so pasted comment drift is invisible to the suite. The emitted text should make clear it replaces the function, not the header.
- **Assert-message size**: `node --test` prints assert.equal's message string in full; embedding a ~30-line function body is fine, and the per-function `it` loop (:318) isolates failures so one failure emits one function's text.
- **Per-directory base resolution is component 2's hard rule**: three resolution contexts verified — repo-root (`${CLAUDE_PLUGIN_ROOT}/...`), skill-local (`skills/ideation/SKILL.md` bare `references/...` → `skills/ideation/references/`, declared :307), sibling (shared refs' bare filenames). A single global base rule produces false positives on the live corpus — the exact Error Handling row ("tune patterns until the live corpus is clean, never the other way around").
- **Anchor checks have three shapes**: `§ N.N` (numbered headings, varying depth — `## 2.` vs `### 5.4`), `core rule N` (numbered list under `### Core rules`, NOT a heading), italic `*Title*` (heading text). Bare numeric refs without § (express:122, :128) are too loose for v1 — skip per the spec's machine-shaped-only rule.
- **Corpus scope is a judgment call**: 17 md files under skills/ + references/ are the minimum; `agents/*.md` (3 files) and `workflows/README.md` are also prompt prose carrying `${CLAUDE_PLUGIN_ROOT}` refs (workflows/README.md:136). Start with skills+references; widen only if clean.
- **The `.gitignore` un-ignore is one line but verify it**: `docs/ideation/*` matches direct children only, so `!docs/ideation/ux-dejank/` suffices — run `git check-ignore -v docs/ideation/ux-dejank/contract.md` (expect exit 1) and `git status --porcelain docs/ideation/ux-dejank/` (expect 12 `??` entries) before committing. Do NOT broaden to `docs/ideation/**` — that would expose pride-report/ and every future working dir.
- **pride-report/ must stay uncommitted**: it is the live workspace of this very project (context-map.md, specs, notes) and execute-spec's commit conventions keep working artifacts out of phase commits. The archive commit adds ux-dejank/ only; `git status` before commit is the guard.
- **asciinema is installed** (`/opt/homebrew/bin/asciinema`; tape/vhs absent) — the optional 90-second capture passes the spec's "only if the tooling is already on the machine" gate. Still optional; cut if it costs more than minutes, and record the decision either way.
- **ux-dejank/contract-data.json criteria shape**: criteria nest under phases with `check` as `{cmd, expect}` objects — if any tooling parses it (archive index, fixture assertions), don't assume the pride-report shape blindly; verify.mjs's normalizeCriterion tolerance (Phase 1) handles both.
- **Lint wire-in budget**: npm test is 756ms for 162 tests; lint-prompts over 17-21 small files will run in milliseconds — the <2s condition for wiring into `test` is safe, but measure anyway (it's a spec condition, not an assumption).
- **The eval component has no precedent to match**: no transcript fixtures exist anywhere in the repo today. The transcripts + manual checklist fallback IS the convention-establishing move — don't gold-plate a subagent driver just to avoid being first. The spec pre-authorizes the fallback ("if the harness can't produce a trustworthy assertion, ship the transcripts + checklist and stop").

## Risks (Phase 5)

- **Spec-vs-reality drift (flagged per protocol; no decision-log contradiction)**: `extract()` at :295-301, not :260; archive reds 3 not 2, one stale-content (Edge Cases). Both carried decisions hold against the tree — bold-moves-last sequencing (Phase 2's single-owner pass and Phase 4's landing did happen and this phase builds on them) and lint-arrives-in-Stretch (nothing implements the rejected lint-first alternative). The drift corrects the archive README's required content; it shrinks no scope.
- **Component 2 is the gold-plating magnet the advisor flagged**: every check beyond paths/anchors/frontmatter (allowed-tools cross-check, prose structure, section-name hardcoding) has verified false-positive sources in the live corpus. The spec pre-rejected the allowed-tools check ("naming conventions are too loose") — hold that line; v1 is paths, § anchors, frontmatter.
- **Lint becoming a second prose owner** (spec Failure Modes, confirmed mechanism): if patterns are hand-copied from today's corpus instead of derived by walking it, the lint fails when prompts legitimately restructure. The lint-pr-title register (read the rules from the config) is the mitigation — derive, don't duplicate.
- **Committing the archive freezes stale claims into searchable history**: ux-dejank's specs/notes describe 0.19.0-era behavior that contradicts current skills. The spec's only mitigation is the dated framing + in-archive README — that paragraph is load-bearing, not polish.
- **The paste-experiment touches production mid-phase**: component 1's acceptance requires intentionally drifting execute-contract.mjs, then fixing it via paste. If interrupted mid-experiment the tree holds a drifted engine. Do the experiment in one sitting; the 162-test net (drift suite + vm smoke harness) runs before the commit.
- **Eval assertions may not survive contact with a live interview** (the spec's own cut line): scoreboard-per-turn and contract-refusal invariants depend on model behavior, not code. Forcing a scripted driver that can't assert honestly is the failure mode — the transcripts + checklist fallback is the designed outcome, not a defeat.

---

# Prior Phase Archive

## Phase 4 (completed)

**Gates**: 5/5 ready — Verdict: GO

| Gate                 | Status    | Evidence |
| -------------------- | --------- | -------- |
| Scope clarity        | ready     | All 6 modified files + 1 new file + 1 deleted directory verified live: `README.md` Installation at :517 (spec said 522 — drift) with `### Claude Code` :519 and **`### pi` :526-532 already present** (see Risks — spec premise contradicted); `site/src/components/InstallBlock.astro` LINES at :15 (CC-only, pi line genuinely missing); `site/src/layouts/Industry.astro` og block :24-28 (no og:image); `site/src/layouts/Guide.astro` og block :15-17 (no og:image, no og:url); `package.json` is the metadata stub (name `ideation-plugin` :2, zero of description/repository/license/keywords/author); `CHANGELOG.md` 0.21.0 section :26-85 with ~40 flat Features, stale version tags, and `updateREADME` at :83; `site/public/` contains only `_headers` + `fonts/` (tracked) — `og.png` is genuinely absent; `docs/ideation/decision-log/` exists and is gitignored (`.gitignore` `docs/ideation/*`), `node scripts/verify.mjs` on its contract-data.json confirms permanently red (fail=2, judgment=1 — spec said fail=1, drift). |
| Pattern familiarity  | ready     | Every pattern source read: `InstallBlock.astro` in full (LINES/payload/caret/copy-both design), both layouts' head blocks, `.claude-plugin/plugin.json` (the metadata donor: description, author Nick Nisi <nick@nisi.org>, 9 keywords), the hand-written CHANGELOG 0.20.0/0.17.0 sections (the curation register: bold-lead bullet + explanation), README's Harness support (:534-549) and Manual Cross-Session Execution (:495-515) sections, `references/harness-compat.md:52` (the CLAUDE_PLUGIN_ROOT caveat's canonical owner), `test-fixtures/orchestration/README.md` and `skills/ideation/examples/` (both link targets exist). |
| Dependency awareness | ready     | Blast radius mapped: InstallBlock ← `site/src/pages/index.astro:183` (variant=primary, caret), `:390` (default secondary), `walkthrough.astro:261` (variant=primary); Industry layout ← index/walkthrough/404 pages; Guide layout ← `site/src/pages/guide.astro` (which renders harness-compat at build via `site/src/lib/harness.ts`); `package.json` name ← `package-lock.json:2,7` only (regenerate via documented `npm i --package-lock-only`), `codebase-pride-report.html` quotes it descriptively (untracked report, no functional dep); release-please already uses `package-name: "ideation"` independent of the package.json name; CHANGELOG ← README header link + release-please appends (curation is safe, future sections land above); decision-log ← nothing tracked (gitignored). |
| Edge case coverage   | ready     | Concrete list in Edge Cases (Phase 4) below: the pi-install premise contradiction (README :526-532 and Harness support :534 already shipped in 0.23.0 — item 1 step 1 is DONE, do not duplicate); the one-owner rule vs the Requirements section (CLAUDE_PLUGIN_ROOT caveat owned by harness-compat.md:52 — point, don't restate); Example section already hoisted at :239 (above the per-skill reference docs at :367+ — only the links line is missing); InstallBlock caret follows `LINES.length - 1` (:36) so extending LINES moves the hero caret to the pi line; 'copy both' payload is per-harness by design; package-lock.json name staleness on rename; CHANGELOG now has 0.22.0/0.23.0 above 0.21.0 (spec's "sits at the top" is stale); Guide.astro og:url asymmetry; verify.mjs fail=2 not fail=1. |
| Test strategy        | ready     | All spec validation commands verified against the live tree: `npm run build` = `pnpm -C site build` and **site deps are present** (`site/node_modules` exists — the Phase 2/3 caveat does not apply this phase); `grep -q 'pi install' README.md` already passes today, InstallBlock leg fails (correctly scopes remaining work); `grep -q 'test-fixtures/orchestration' README.md` and `grep -q 'examples/' README.md` both fail today; the `node -e` package.json assertion fails today (all five keys absent); og:image grep fails today on both layouts; `node scripts/verify.mjs ./docs/ideation/pride-report/contract-data.json` exists for criteria 16-21. og capture loop: `npm run dev` (= `pnpm -C site dev`, port 4321) + browser capture of ContractPlate at 1200×630. |

### Key Patterns (Phase 4)

- `site/src/components/InstallBlock.astro:15-18` — **the one-clipboard-payload design**: `LINES` array → `payload = LINES.join('\n')` → single `data-copy` button labeled "copy both". Header comment (:16-17): "One clipboard payload, both lines — pasted together they register the marketplace and install from it in a single go." The pi addition must preserve one payload *per harness* — either extend LINES (breaks the per-harness semantics, moves the caret) or add a second labeled block (spec's preferred option; keeps the CC payload byte-identical per the Failure Modes table).
- `InstallBlock.astro:31-38` — **caret mechanics**: renders after the line where `i === LINES.length - 1`, only when `caret` prop set (hero usage at index.astro:183). Extending LINES silently moves the hero's blinking caret to the pi line — a visual regression the build won't catch.
- `site/src/layouts/Industry.astro:24-28` — **the og-meta block shape**: hand-written `<meta property="og:*">` lines in frontmatter-built head; `canonical` uses `new URL(Astro.url.pathname, Astro.site)` and `Astro.site` = `https://ideation.engineering` (`site/astro.config.mjs:10`). The og:image line (`content="https://ideation.engineering/og.png"`) joins this block. Guide.astro:15-17 has the same block minus `og:url` (asymmetry predates this phase; spec only asks for og:image).
- `.claude-plugin/plugin.json` — **the metadata donor**: `description` (one long sentence — spec says trim), `author` {Nick Nisi, nick@nisi.org}, 9 `keywords` to copy verbatim into package.json. `repository: "github:nicknisi/ideation"` and `license: "MIT"` per spec (no LICENSE file check needed — spec asserts MIT).
- `CHANGELOG.md:87-94` (0.20.0 section) — **the curation register**: bold-lead bullet naming the user-visible change, then the explanation and the why, links to README anchors. "extend that standard upward by one section" means rewriting 0.21.0's :28-83 into 5-6 of these, keeping the compare link at :26 and the generated-vs-handwritten preamble at :1-5.
- `references/harness-compat.md:52` — **the CLAUDE_PLUGIN_ROOT caveat's canonical owner**: names Nick's user-level `claude-plugin-root.ts` extension as not bundled, plus the fallback resolution (relative to skill directory). The README Requirements section must *point here*, not restate — the repo's own one-owner rule (README :551-574, added Phase 2) makes a second prose copy the exact rot this project fixes.
- `README.md:495-515` — **Manual Cross-Session Execution section**: the manual fallback already exists as a section; the spec wants it *framed* as "the fallback if you lack Workflows" and the Requirements section to name which commands need the engine (autopilot, express, get-goal-prompt). harness-compat.md:18 already owns the degradation sentence ("If the Workflow tool is unavailable in either harness, autopilot degrades to the manual per-phase path").
- `README.md:602+` — **Releasing section register**: states the release-please mechanics then the sharp edge ("Note the last row: marking a type `hidden`…"). The one added line (generated sections get a final human edit in the release PR) follows this register.

### Dependencies (Phase 4)

- `site/src/components/InstallBlock.astro` — consumed by → `site/src/pages/index.astro:183` (`variant="primary" caret` — the hero), `index.astro:390` (bare, secondary), `site/src/pages/walkthrough.astro:261` (`variant="primary"`). All three render the same LINES; a second harness block appears in all three places automatically. No props change needed for the spec's options.
- `site/src/layouts/Industry.astro` — consumed by → `/` and `/walkthrough` (and 404) pages; og:image addition is additive meta, zero consumer impact.
- `site/src/layouts/Guide.astro` — consumed by → `/guide` (`site/src/pages/guide.astro`, which builds from `references/harness-compat.md` via `site/src/lib/harness.ts`). Additive meta only.
- `package.json` `"name": "ideation-plugin"` (:2) — consumed by → `package-lock.json:2,7` (lockfile name fields — regenerate with `npm i --package-lock-only`, the command README :598-600 documents; pi's git-clone install runs `npm install` and respects the lockfile per `.gitignore`'s own comment). `codebase-pride-report.html` mentions the name descriptively (untracked, no action). release-please-config.json already declares `package-name: "ideation"` for changelog tagging — independent of the package.json name, unaffected either way. pi install resolves by git URL, not name; Claude Code resolves the plugin by `.claude-plugin/plugin.json` (`"name": "ideation"` there already). **The grep the spec mandates proves the rename safe; the lockfile regen is the one required follow-on edit the spec's File Changes table doesn't list.**
- `CHANGELOG.md` — consumed by → README header link, GitHub release notes (release-please copies sections), and future release-please appends (new generated sections land *above* 0.23.0; the curated 0.21.0 stays put). Curation cannot break automation — release-please only prepends.
- `README.md` Installation/Harness support block (:517-549) — consumed by → strangers; the guide page renders harness-compat independently, so README edits don't drift the site.
- `docs/ideation/decision-log/` — consumed by → nothing tracked. Gitignored via `.gitignore` `docs/ideation/*` (only `learnings.md` un-ignored). Deletion is local-only; `node scripts/verify.mjs docs/ideation/decision-log/contract-data.json` currently reports `fail=2 judgment=1` on paths/commits that no longer exist — the retirement rationale is unaffected — if anything stronger. Also note the directory contains `implementation-notes-phase-2.html` and two specs — all ignored, all local.
- `site/public/og.png` (new) — consumed by → the two layouts' new meta tags, GitHub social preview (manual Open Item), Slack/og-preview unfurls. Astro copies `public/` verbatim into `dist/`; `site/public/_headers` needs no change for a png.

### Conventions (Phase 4 additions; Phase 1-3 conventions still apply)

- **Astro component authoring**: frontmatter with `interface Props` + JSDoc per prop, derived `const`s, header comments that state the *design intent* ("One clipboard payload, both lines…"), Tailwind class strings composed in consts (see InstallBlock's `button` const :20-24). New harness labeling should carry an intent comment in the same voice.
- **One-owner rule is now load-bearing for prose** (README :551-574, landed Phase 2): the Requirements section points at `references/harness-compat.md` for the CLAUDE_PLUGIN_ROOT caveat and the extension matrix rather than restating them. The spec's step 3 wording ("name it as the one piece of Nick's own setup not bundled") is a pointer+one-sentence, not a re-explanation.
- **README voice** (from Phase 2, still binding): state the rule, then the failure it prevents. Installation subsections are `### Claude Code` / `### pi` — the Requirements section slots as a sibling `##`-level or trailing paragraph per the builder's read of the surrounding hierarchy.
- **Changelog curation register**: hand-written sections use bold-lead bullets with the user-visible change first, mechanics second, links to README anchors; stale version tags in commit subjects ("(v0.17.0)") and noise subjects ("updateREADME") are exactly what the curation drops.
- **package.json metadata**: mirror `.claude-plugin/plugin.json` values; keep `private: true`, zero dev-dependencies, and the committed-lockfile story (`/pnpm-lock.yaml` ignored, `package-lock.json` committed — `.gitignore` comment block explains why).
- **Site deploy**: Cloudflare builds from `site/`; `site/public` assets ship at the domain root. Absolute og:image URL on the production domain per spec (Astro.site config confirms `https://ideation.engineering`).

### Edge Cases (Phase 4)

- **The spec's central premise is stale — scope item 1 down to the delta.** "The word 'pi' appears zero times in README.md and the site" was true at contract time; 0.23.0 (`c1a4309`, "full pi harness support — seamless install") shipped `### pi` install at README :526-532, the Harness support section :534-549, and the guide page rendering harness-compat (`site/src/lib/harness.ts`). Verified still-missing: (a) the pi line in `InstallBlock.astro` (LINES :15 is CC-only), (b) a Requirements section naming the CLAUDE_PLUGIN_ROOT caveat and workflow-engine commands for strangers (no `## Requirements` exists; the caveat lives only at harness-compat.md:52), (c) the fallback framing. The check commands remain the source of truth: the README leg of `grep -q 'pi install' …` already passes — do NOT add a duplicate pi line to README.
- **Caret migration in InstallBlock**: `caret && i === LINES.length - 1` (:36) — appending the pi line to LINES moves the hero's blinking caret off the CC payload. The spec's "second block with a harness label" option avoids this entirely; if extending LINES, the hero visual changes and "copy both" copies a two-harness payload (the exact Failure Mode the spec names).
- **Example hoist is already done; only links are missing.** `## Example` sits at :239, above the per-skill reference docs (`### /ideation:execute-spec` :367, autopilot :403, get-goal-prompt :449). Spec's "(~line 380)" anchor drifted. Remaining work: one compact line linking `test-fixtures/orchestration/` (README exists there, "a synthetic ideation project… the dependency graph is the point") and `skills/ideation/examples/` (contract/prd/spec-example.md). Acceptance grep needs the literal strings `test-fixtures/orchestration` and `examples/`.
- **package-lock.json on rename**: `package-lock.json:2,7` carry `"name": "ideation-plugin"`. The spec's grep gate (`rg 'ideation-plugin'`) will keep hitting the lockfile after a package.json-only rename. Regenerate with `npm i --package-lock-only` (documented at README :598-600) or the lockfile and package.json disagree. `codebase-pride-report.html` also matches the grep but is the untracked report itself — exclude from the decision.
- **CHANGELOG topography drift**: 0.22.0 (:17-24) and 0.23.0 (:7-15) now sit above 0.21.0 — the spec's "sits at the top of the second link in the README header" is stale; 0.21.0 is still the worst section and the only curation target. Keep: the preamble (:1-5), the 0.21.0 compare link (:26), and every hand-written section from 0.20.0 (:87) down. Do not "curate" 0.22.0/0.23.0 — they're already tight.
- **Guide.astro og asymmetry**: it lacks `og:url` (Industry has it at :28) as well as og:image. Spec mandates og:image only; adding og:url is out of scope — flag in phase notes, don't fix silently.
- **decision-log verify count drift**: spec says `fail=1`; reality is `fail=2, judgment=1` (both spec-phase-1/2 commit references dead). The retirement rationale is unaffected — if anything stronger. Also note the directory contains `implementation-notes-phase-2.html` and two specs — all ignored, all local.
- **Requirements vs Harness support overlap**: README :534-549 already says "targets Claude Code and runs in pi" and points at harness-compat. A new Requirements section must complement (install prerequisites: what a stranger needs beyond `pi install`), not re-state the harness matrix. The one-owner table (:558-573) is watching.
- **og.png production is the only non-mechanical item**: 1200×630 capture of ContractPlate (`site/src/components/ContractPlate.astro` exists; the walkthrough renders it) via `npm run dev` at localhost:4321. Verify beyond `test -f`: `npm run build` must show it in `dist/`, and the manual og-preview check is on the checklist. No screenshot replaces the drawn plates on the page (spec constraint).
- **Releasing-section one-liner placement**: the section (:602+) already explains release-please writes CHANGELOG.md; the added line (generated sections get a final human edit in the release PR) belongs near the "merging it tags the release… and writes CHANGELOG.md" sentence, in the same state-the-rule voice.

### Risks (Phase 4)

- **Spec-premise contradiction (readiness-relevant, flagged per protocol)**: the Technical Approach's framing premise — "the repo quietly became pi-installable… while every install instruction still assumes Claude Code; the word 'pi' appears zero times in README.md and the site" — is contradicted by the live tree (README :526-549, guide page, harness-compat.md all shipped in 0.23.0). This is not a decision-log entry (the three carried decisions — sequencing, ux-dejank preserved for Phase 5, tiered scope — all still hold against the tree), but it shrinks item 1 to the InstallBlock line + Requirements delta. If the builder follows the spec's step 1 literally, they duplicate an existing section. The spec's own check commands correctly scope the real work — trust them over the prose.
- **Two-harness clipboard confusion** (spec Failure Modes, confirmed live): one `data-copy` payload per InstallBlock instance; mixing harness lines into one payload pastes pi syntax into Claude Code. Keep the CC payload byte-identical; label by harness.
- **Rename follow-through**: package.json rename without lockfile regen leaves `package-lock.json` naming `ideation-plugin` — pi's `npm install` on clone respects the lockfile, so the drift is user-visible. The spec's File Changes table doesn't list package-lock.json; the grep gate forces it.
- **og.png is hand-made**: no cmd-check verifies it looks right — only existence and dist inclusion. The Manual Testing checklist (og preview) is the real gate; a rushed screenshot ships a bad first impression on the project's most-shared URL.
- **Curation is judgment**: the 0.21.0 rewrite has no mechanical check beyond keeping the compare link; the spec's criterion ("read the result as a stranger judging maintenance health") is the only gate. Over-trimming loses real user-visible changes; under-trimming keeps the noise.
- **Requirements section rot class** (spec Failure Modes, accepted): naming the three extensions in README creates a second prose copy of the package.json manifest. The spec's mitigation (name the manifest fields so drift is greppable) is the right one — keep the package names in a form `grep -f <(node -p …)` could diff.

---

## Phase 3 (completed)

**Gates**: 5/5 ready — Verdict: GO

| Gate                 | Status    | Evidence |
| -------------------- | --------- | -------- |
| Scope clarity        | ready     | All 9 modified files + 1 new file exist and every cited anchor was verified live: `.gitignore:9` (`DESIGN.md` — confirmed by `git check-ignore -v`); `DESIGN.md:3` ("already present in `docs/index.html`"); `docs/index.html` is the 29-line redirect stub (spec: "850-byte redirect stub" ✓); `scripts/contract-gen.css:9` ("inherited from docs/index.html"); `site/src/styles/global.css:6` ("copied verbatim from the marketing page (public/index.html)"); `skills/ideation/references/html-guide.md` is exactly 1,165 lines with the false canonical claim at `:4`; `skills/execute-spec/references/implementation-notes.template.html:18,21` (`--fg: #1a1a2e`, `--accent: #0f3460`); `README.md:353-358` mermaid style lines (spec said 352-357 — minor drift, see Edge Cases); `scripts/contract-gen.ts:844` `function computeWaves` (spec said 845 — one-line drift) with its only call site at `:366`; `site/README.md` has the `## Design` section the new line joins. New file `scripts/design-tokens.test.mjs` is picked up by the existing `npm test` glob (`scripts/*.test.mjs`, package.json:30). |
| Pattern familiarity  | ready     | Every "Pattern to follow" read: `DESIGN.md` in full (the voice to keep — "an eyebrow over every section is grammar nobody chose" at :50; the token table at :20-34 is the reskin source; the incident quote the palette-test comment needs is at :8) and `workflows/wave-planner.test.mjs:278-300` (the drift-suite register: "This test is the enforcement.", readFileSync + extract idiom). The two-worlds reality is confirmed in `site/src/styles/industry.css:3-13` and `site/README.md` `## Design`. |
| Dependency awareness | ready     | Blast radius mapped: `computeWaves` in contract-gen.ts ← only `:366` (`const waves = computeWaves(phases);`), zero test references by name (grep-verified); html-guide.md ← `README.md:107`, `skills/ideation/SKILL.md:307`, `:319`, `:326` (all already scoped to ephemeral comparisons); notes template ← `skills/execute-spec/SKILL.md:92` (copy-verbatim instructions; structure untouched = no consumer impact); `DESIGN.md` ← nothing tracked today (only `.gitignore:9` and `codebase-pride-report.html:268` mention it); `global.css` ← `/guide` pages only (industry.css:11-13 confirms the two worlds never meet in one document). |
| Edge case coverage   | ready     | Concrete list in Edge Cases (Phase 3) below: six mermaid style lines at 353-358 (not 352-357) with `REVIEWER` using non-renounced `#0a1628`/`#53a653`; the template's dark values live in a `prefers-color-scheme` media query, not `data-theme` blocks (spec says "both light and dark data-theme blocks" — inaccurate); palette-test self-exemption; html-guide §4.12 Implementation Notes duplicates the template (safe deletion, template owns); `SKILL.md:307`'s "component library, design tokens" wording post-collapse; `git add` ordering vs the `.gitignore` line; the decision-aid section (`SKILL.md:317-319`) as behavioral owner cross-check. |
| Test strategy        | ready     | Inner loop `node --test scripts/design-tokens.test.mjs` (new; auto-globbed). Full gate `npm test` (currently 161 green) covers the rename via `scripts/contract-gen.test.mjs` (indirect — exercises graph HTML output, no name references). `npm run build` = `pnpm -C site build` guards the global.css header edit. Palette census grep verified against the live tree: exactly the hits the spec predicts (README 353-357, notes template 18/21, html-guide 116/117) and zero hits in `workflows/`, `site/`, or `docs/` outside the walk scope. Visual loop: `/tmp/notes-preview.html` + `open` per spec. |

### Key Patterns (Phase 3)

- `DESIGN.md:20-34` — **the token table, the reskin source of truth**: light `--bg #f7f7f3` / `--ink #191b1d` / `--accent #2b46c7` / `--muted #50555a` / `--line #dededf`; dark `--bg #131417` / `--ink #e9e9e4` / `--accent #93a7ff` / `--muted #a8aaa6` / `--line #2d3036`. The notes-template reskin maps its five tokens (`--bg/--fg/--muted/--border/--accent`) onto these values, both blocks. "The values are the contract" (:23).
- `DESIGN.md:6-9` — **the incident quote for the palette-test comment**: "the contract renderer previously shipped a completely separate dark 'command deck' world, and that mismatch was the single biggest reason a generated contract did not read as part of this product." The spec's required test header paraphrases this.
- `workflows/wave-planner.test.mjs:278-284` — **the drift-test comment register**: names the rot mechanism, cites the prior failure, ends "This test is the enforcement." The palette guard's header mirrors this shape. Its mechanics (readFileSync over repo files, assert over content) are the template for walking `skills/`, `scripts/`, `README.md`.
- `site/src/styles/industry.css:3-13` — **the two-worlds declaration already on disk**: "A different world from the field guide (src/styles/global.css)… Only /(index) and /walkthrough import this. /guide still imports global.css and keeps its paper-and-serif world; the two never meet in one document." DESIGN.md's new "Two worlds" section should align with this wording, not reinvent it. Slate-blue `#5980a6` + Barlow per the spec.
- `site/README.md` `## Design` section — **already documents both worlds** ("Two systems live here, one per stylesheet, and they never meet in one document.", the Field guide paragraph naming `/guide`). Step 7's one line is a *decision record* (deliberate + revisit condition), not new documentation — append to the Field guide paragraph.
- `skills/execute-spec/references/implementation-notes.template.html:2-7` — **header comment declaring single ownership**, must survive verbatim; the style block is :14-58 (~44 lines, spec said ~30). Structure: one `:root` block (:15-22) + one `@media (prefers-color-scheme: dark)` block (:23-30). There is **no** `[data-theme='dark']` selector — `data-theme="auto"` on `<html>` (:9) is inert (no JS in the file). "Keep the data-theme scaffold" in practice means: don't touch the attribute or the media query, swap values only.
- `skills/ideation/SKILL.md:317-319` — **Decision Aids section, the behavioral owner of comparisons**: the `_comparison.html` workflow (option as card/column with name, description, trade-offs, visual where apt; `open`; AskUserQuestion; delete after the choice). The collapsed html-guide's surviving 3-4 components (option cards, trade-off lists, copy snippet) must match this workflow — cross-check before deleting anything per the spec's Error Handling table.
- `scripts/contract-gen.ts:842-843` — **existing comment above `computeWaves`**: "Wave (column) per phase: longest prereq chain depth. When no phase declares prereqs, the plan is an implicit sequential chain." The rename's new two-line "deliberately NOT the dispatch planner" comment joins this block; the cycle-guard comment at :852 (`// cycle guard: treat as root`) is the "different cycle semantics" the comment should name.

### Dependencies (Phase 3)

- `scripts/contract-gen.ts:844` (`computeWaves` → `computePhaseDepths`) — consumed by → `scripts/contract-gen.ts:366` (`const waves = computeWaves(phases);` — the ONLY call site; both must change together). No test references the name (grep-verified against `scripts/contract-gen.test.mjs`). The rename's *reason* is the collision with the canonical planner: `workflows/wave-planner.mjs:75` exports the real `computeWaves`, mirrored verbatim into `workflows/execute-contract.mjs:115`, named in `workflows/README.md:12,16,23,161` and throughout `workflows/wave-planner.test.mjs`. None of those import contract-gen.ts — they are unaffected by the rename, and the rename must not touch them.
- `skills/ideation/references/html-guide.md` — consumed by → `README.md:107` (listing, already says "for ephemeral comparison/visualization artifacts"), `skills/ideation/SKILL.md:307` (Bundled Resources listing), `:319` (decision-aid workflow: "using `references/html-guide.md` components"), `:326` ("Read `references/html-guide.md` before any ephemeral HTML"). All four references survive the collapse intact — they already scope it to ephemeral artifacts.
- `skills/execute-spec/references/implementation-notes.template.html` — consumed by → `skills/execute-spec/SKILL.md:92` (copy-verbatim-on-first-entry instructions; declares the template "the single owner of the file shell and the note-entry format"). Value-swap only = zero consumer impact. Note: `html-guide.md` §4.12 "Implementation Notes" (:1017-1056) is a stale duplicate of this format — its deletion in the collapse is safe because the template owns it.
- `DESIGN.md` — consumed by → nothing tracked today. After this phase: `scripts/contract-gen.css` header and `site/src/styles/global.css` header cite it as authority (step 1.4), and the collapsed html-guide points at it. It becomes a load-bearing doc — future edits to it now have three dependents.
- `site/src/styles/global.css` — consumed by → `/guide` pages only (industry.css:11-13, site/README.md route table). The header-comment edit is comment-only; Tailwind build unaffected.
- `README.md` mermaid block (:316-358) — consumed by → GitHub rendering, light and dark mode (the spec's argument for dropping the style lines: hardcoded dark fills read poorly in light mode). No build dependency.
- No external consumers for `.gitignore`, `docs/index.html` (stays a redirect stub; it legitimately keeps field-guide values and is outside the palette walk).

### Conventions (Phase 3 additions; Phase 1-2 conventions still apply)

- **Design-token authority**: values live in exactly one place and everything else points. After this phase the chain is: `DESIGN.md` (authority) → `contract-gen.css` + `site/src/styles/global.css` (declarations) → generated artifacts (inherit). The html-guide collapse converts the worst violator (a second, renounced token table) into a pointer.
- **CSS header blocks**: the banner-comment register (`═══` rules, THE SIS/WORLD/REGISTER/MOTION labels in contract-gen.css:1-22; the shorter paragraph form in global.css:3-13 and industry.css:3-13). Citation fixes stay in this register — name the authority, don't re-derive the values.
- **Drift-test header comments**: name the incident, cite the prior failure or renounced claim, end "This test is the enforcement." (`wave-planner.test.mjs:278-284`, `contract-gen.test.mjs:6`).
- **Test conventions** (from Phase 1, still binding): `node --test` + `node:assert/strict`, ESM `.mjs`, `node:`-prefixed builtins, tests beside modules as `<module>.test.mjs`. The palette test walks the tree with `node:fs`/`node:path` relative to `import.meta.url` (the drift suite's idiom at wave-planner.test.mjs:285-287).
- **Mermaid in README**: style lines are cosmetic-only; dropping them is an endorsed option ("Decide by rendering"). If replaced, field-guide values: paper fill `#f7f7f3`, ink stroke/text `#191b1d`, cobalt `#2b46c7` for accent nodes.
- **Decision records in READMEs**: site/README.md already records decisions with revisit conditions ("Do not put an `index.html` back in `public/` — Vite special-cases it…"). Step 7's line follows this register: state the decision, the reason, the revisit trigger.

### Edge Cases (Phase 3)

- **Mermaid line drift and count**: the six `style` lines are `README.md:353-358`, not 352-357. Five use the renounced palette; the sixth (`:358` `style REVIEWER fill:#0a1628,stroke:#53a653,color:#fff`) uses `#0a1628`/`#53a653`, which are NOT in the renounced four — the acceptance grep passes even if :358 survives. If replacing (not dropping), all six lines need field-guide values for visual coherence; if dropping, drop all six.
- **The template's dark values live in a media query, not a `data-theme` block.** Spec step 3 says "both light and dark `data-theme` blocks" and "keep the existing `data-theme` scaffold" — reality: `:root` (:15-22) + `@media (prefers-color-scheme: dark)` (:23-30); `data-theme="auto"` on `<html>` is inert (no JS). Swap values in both blocks; add nothing structural. The spec's preview step ("toggle data-theme light/dark") won't actually toggle anything in the standalone template render — preview in a dark-mode browser/OS or temporarily hardcode the media query.
- **Reskin value mapping** (five tokens, both blocks): `--bg`→`#f7f7f3`/`#131417`, `--fg`→`#191b1d`/`#e9e9e4`, `--muted`→`#50555a`/`#a8aaa6`, `--border`→`#dededf`/`#2d3036`, `--accent`→`#2b46c7`/`#93a7ff`. The template's light `--bg` is currently `#ffffff` — paper is `#f7f7f3` (surface-1 `#fcfcfa` is the alternative; paper matches the world's ground rule). Fonts stay system-ui per "values swap, not a rewrite."
- **Palette-test self-exemption**: `scripts/design-tokens.test.mjs` contains all four literals; exclude it from its own walk (spec Failure Modes) — compare resolved paths, not substrings, so a renamed test file can't silently re-include itself.
- **Walk-scope completeness verified**: zero renounced-palette hits in `workflows/`, `site/`, or `docs/` (excluding `docs/ideation/pride-report/`, which legitimately quotes the palette in specs). The spec's three-path walk (`skills/`, `scripts/`, `README.md`) covers every live hit. `codebase-pride-report.html` at root quotes the palette descriptively but is outside the walk — correct, it's the report itself.
- **html-guide §4.12 Implementation Notes (:1017-1056)** duplicates the notes template's `.note-entry` format with old-token values. Deleting it in the collapse is safe — execute-spec/SKILL.md:92 names the template the single owner — but the builder should not "rescue" it into the collapsed file.
- **`SKILL.md:307` wording check**: after collapse, html-guide is "~100 lines: ephemeral comparisons only + pointer + 3-4 components." The listing's current description — "component library, design tokens, constraints — for ephemeral comparison artifacts only" — overstates "design tokens" once the token tables die. Spec step 2.3 says verify, no edit required; a one-word trim ("components, constraints") is the honest fix if the builder judges it needed — flag in phase notes either way.
- **`git add DESIGN.md` ordering**: remove the `.gitignore:9` line FIRST, then `git add` (or use `git add -f`). `git check-ignore -v DESIGN.md` currently confirms the ignore (`.gitignore:9`). The acceptance command is `git ls-files --error-unmatch DESIGN.md`.
- **DESIGN.md citation fix**: the file's own opening (:2-4) claims the identity is "already present in `docs/index.html`" — the rewrite names `contract-gen.css` + `global.css` as canonical declarations. Note `docs/index.html` (the redirect stub) still carries the field-guide values in its inline styles — it remains a legitimate *user* of the tokens, just not the authority. The check grep `! grep -q 'docs/index.html' DESIGN.md scripts/contract-gen.css` demands zero mentions in both files.
- **Rename completeness**: both `contract-gen.ts:844` (definition + JSDoc above it) and `:366` (call site) must change. A missed call site throws ReferenceError at graph build — caught by `npm test` (contract-gen tests exercise the graph HTML), but the check grep `grep -n 'computePhaseDepths' scripts/contract-gen.ts` alone would pass with only the definition renamed. Run the full grep pair the spec gives.
- **Two-worlds wording alignment**: DESIGN.md's new section, industry.css:3-13, and site/README.md's Design section will all describe the same two worlds — align terminology ("field guide" vs "Industry", which routes wear which) rather than introducing a third phrasing.
- **`npm run build` needs site deps** (`pnpm -C site install` if `site/node_modules` is absent) — same caveat as Phase 2; the global.css edit is comment-only so a skipped build is low-risk but still a verification gap. (Phase 4 note: site deps are now present.)

### Risks (Phase 3)

- **DESIGN.md becomes load-bearing the moment it is tracked.** Today nothing references it; after this phase three files cite it as token authority (contract-gen.css, global.css, html-guide). Future token drift in DESIGN.md now propagates by prose — there is no mechanical check that `contract-gen.css`/`global.css` values match DESIGN.md's table (the palette test only guards the renounced values). Acceptable per the spec's scope; flag if a follow-up phase wants a token-parity test.
- **The palette guard's walk is path-scoped, not repo-wide.** A future artifact under `workflows/`, `site/`, or a new top-level directory can reintroduce the renounced values undetected (verified: those paths are clean today). The spec accepts this; the test comment should name the walk scope so nobody reads it as repo-wide.
- **Mermaid rendering decision is visual judgment, not cmd-checkable.** "Replace vs drop" is decided by rendering on GitHub in both modes — the builder cannot fully verify dark-mode rendering locally. The spec endorses either outcome; the safer default is dropping the style lines (mermaid defaults adapt to GitHub's theme).
- **Spec-vs-reality inaccuracies found (none blocking)**: mermaid lines 353-358 not 352-357; `computeWaves` at :844 not :845; template dark values in a media query not `data-theme` blocks; style block is ~44 lines not ~30. All target strings verified unique — cut by content, not line range.
- **Notes reskin dark-theme legibility** (spec Failure Modes): both blocks must be previewed; the media-query dark block can't be toggled by `data-theme` in the standalone preview (see Edge Cases) — use OS dark mode or a temporary media-query override.

---

## Phase 2 (completed)

**Gates**: 5/5 ready — Verdict: GO

| Gate                 | Status    | Evidence |
| -------------------- | --------- | -------- |
| Scope clarity        | ready     | All 11 modified files and 2 deleted files exist, and every cited anchor was verified live: `README.md:97` ("Shared (plugin root)"), `:106` (contract-template listing), `:185-192` (gate table); `references/interview-engine.md:147-155` (gate-tracking table); `references/confidence-rubric.md:9-11` (`## Question cadence`), `:140` ("see Question cadence" pointer); `references/harness-compat.md:32` (compat-shim parenthetical), §3 table, "What does NOT differ" bullet; `references/feedback-loop-guide.md:64` ("(Phase 2.2)"); `skills/ideation/SKILL.md:32` (Phase 3 opener — spec said ~64, actual :32, string unique), `:68` (schema comment), `:108` (meta-aside), `:208` (§5.1 row), `:218` (§5.2 bullet), `:308` (Bundled Resources); `skills/autopilot/SKILL.md:76` (matrix pointer), `:183` (Learning Capture); `skills/execute-spec/SKILL.md:35-46` ("prose owner" + matrix), `:224-226` (Validation Results rows), `:242` (Learning Capture); `scripts/contract-gen.ts:168`, `:1425-1435`, `:1687-1696`. Minor spec line drift noted in Edge Cases — every target string is unique, so all changes are concrete. |
| Pattern familiarity  | ready     | Every file the spec touches was read in full or at the cited ranges: the canonical sources to align to (`confidence-rubric.md` `**Gate question**` lines, `interview-engine.md` core rule 1 at :101, `workflows/README.md` "### Gate behavior" table with the scout-unavailable row), the dedupe victims (both Learning Capture sections), and the verification layer (`site/src/lib/gates.ts`, `site/src/lib/skills.ts`). Conventions to replicate are in the Conventions section. |
| Dependency awareness | ready     | Blast radius mapped: rubric → `site/src/lib/gates.ts` (parses `### Gate:` headings, throws unless exactly 5, requires `**Gate question**` + `**Ready when**` per gate); SKILL frontmatter → `site/src/lib/skills.ts` (frontmatter-only; body prose edits are invisible to it); execute-spec matrix ← `autopilot:76`, `autopilot:112`, `autopilot:205`, `ideation:129` (four prose pointers name execute-spec as owner — spec only names :76); contract-template.md ← `README.md:106`, `ideation/SKILL.md:308`, **and `contract-gen.ts:1470` comment** (see Edge Cases — the spec's deleted-files claim "referenced by nothing but listings" is wrong about the comment); `agentTeamPrompt` ← nothing else (verified: no test, fixture, or site reference). |
| Edge case coverage   | ready     | Concrete list in Edge Cases (Phase 2) below: weak `'Also seen in'` grep (line-wrapped at execute-spec:261 — passes today), the `contract-gen.ts:1470` dangling comment the acceptance grep forces, the pointer-to-pointer chain for the three matrix references the spec doesn't name, the ambiguous "matching shim phrasing" scope (one §3 row vs three), gates.ts regex body-capture semantics for the cadence deletion, qualifier preservation ("watched"/"interactive runs only"), and render-block boundary drift in contract-gen.ts. |
| Test strategy        | ready     | Verified live: `npm test` → 161 tests, 0 fail, 1.2s (baseline green post-Phase-1; contract-gen tests included and no fixture references `agentTeamPrompt`). Drift test: `npm run build` (site build; `gates.ts` throws unless exactly 5 gates parse from the edited rubric — not run by scout because it writes `site/dist`, but the mechanism is evidenced by reading gates.ts/skills.ts). Per-item greps enumerated in the spec's Validation Commands all run clean against the current tree, and `docs/ideation/pride-report/contract-data.json` exists for the final `node scripts/verify.mjs` criterion check. |

### Key Patterns (Phase 2)

- `references/confidence-rubric.md:17,37,57,79,99` — **canonical gate wording**: the five `**Gate question**:` lines are the verbatim source both tables must match. Only two rows actually diverge today: `README.md:187` ("…who has it, and why?" — missing "it matters") and `interview-engine.md` gate-tracking table (Problem Clarity missing "who has it"; Success Criteria reads "Can I write tests or validation steps for "done"?" vs rubric's "Can every stated goal be checked pass/fail today?"). Goal Definition, Scope Boundaries, and Consistency already match in both files.
- `references/interview-engine.md:101` — **core rule 1**, the cadence owner the rubric's deleted section must point at. The rubric's `## Question cadence` (:9-11) restates it almost verbatim; replacement is one line: "Question cadence is owned by `interview-engine.md` core rule 1." The :140 pointer ("(see Question cadence)") retargets to the engine.
- `references/learning-filter.md:60-100` — **already owns the lifecycle mechanics** the spec asks it to absorb: bootstrap header (:60-67), "Also seen in" dedupe (:76-84), verified retirement (:86-96), dated entry format (:98-108). "Absorbing the canonical step" means the two skill sections shrink to qualifier + one sentence + pointer; the reference file itself needs little or no new content — verify the procedure is complete there, don't duplicate it.
- `workflows/README.md` "### Gate behavior" table — **the surviving matrix copy**, already more complete than execute-spec's (has the "Scout unavailable (crash/no result)" row). Add one owner-declaration line above it mirroring execute-spec:35's current wording; the table itself does not change.
- `skills/execute-spec/SKILL.md:35-46` — **the self-declared "prose owner" block** to replace with the pointer sentence the spec dictates ("Gate semantics are owned by `workflows/README.md`'s gate-behavior table; when prose and engine disagree, the tests are right."). Keep :159's `WARNING — UNREVIEWED CODE COMMITTED` degradation description — it is local context, not matrix.
- `site/src/lib/gates.ts:26-47` — **the drift test for the rubric edit**: regex `### Gate: (.+?)\n([\s\S]*?)(?=\n### Gate: |$)` — the last gate's body captures to EOF, and each gate body must contain `**Gate question**:` and `**Ready when**:` or the build throws. Deleting `## Question cadence` (an H2 *before* `## Gates`) cannot affect this; deleting or retitling any `### Gate:` heading fails the build. This is why the cadence cut is safe and why `npm run build` is the proof.
- `scripts/contract-gen.ts:1553-1562` (`mdDecisions`) — **the load-bearing "None recorded." semantic** (`:1561`, spec said 1555) that justifies deleting contract-template.md; also carried by `spec-template.md:26` and `:302`. Verified before deletion per spec step 8.1.
- `skills/autopilot/SKILL.md:183-201` and `skills/execute-spec/SKILL.md:242-263` — **the two Learning Capture sections** to reduce. Target shape per spec: qualifier heading line stays ("watched runs only" / "interactive runs only"), one sentence on when it runs, pointer to `learning-filter.md`. The qualifier difference is the hardest rule of the filter (unattended runs never prompt/write) — it must survive verbatim.

### Dependencies (Phase 2)

- `references/confidence-rubric.md` — consumed by → `site/src/lib/gates.ts:11` (build-time parse, throws on ≠5 gates), `references/interview-engine.md:147` (defers to it for gate criteria), `README.md:197` ("The full criteria live in `confidence-rubric.md`"), `skills/ideation/SKILL.md:29` (Phases 1-2 pointer) and 4.5 (Spec Feedback Quality checklist).
- `references/interview-engine.md` core rule 1 — consumed by → `references/confidence-rubric.md` cadence section (being replaced by a pointer back — after the edit the ownership is one-directional, engine owns, rubric points).
- `skills/execute-spec/SKILL.md` resolution matrix — consumed by → `skills/autopilot/SKILL.md:76` ("do not restate them here"), `:112` ("per execute-spec's resolution matrix"), `:205` ("semantics: execute-spec's resolution matrix"), `skills/ideation/SKILL.md:129` ("per execute-spec's headless/strict resolution matrix"). After the swap all four point at a pointer unless retargeted (spec names only :76 — see Edge Cases).
- `references/harness-compat.md` §2 — consumed by → every `Agent`/`subagent` dispatch instruction in `skills/ideation/SKILL.md` (steps 4, 5.1-adjacent), `skills/autopilot/SKILL.md` Step 3/4, `skills/execute-spec/SKILL.md` Scout/Review sections. The translation rule stands alone after the parenthetical cut — no consumer quotes the shim claim (grep-verified: "compat shim" appears only at harness-compat.md:32).
- `scripts/contract-gen.ts` — consumed by → `scripts/contract-gen.test.mjs` (must stay green; no fixture references `agentTeamPrompt` — verified), `skills/ideation/SKILL.md` Phase 3 step 5 / 5.2 (invocation), autopilot fallback parser (reads its `contract.md` output structure — the markdown Agent Team Prompt block removal does not touch `**Approval**`, dependency graph, or per-phase command lines the parser needs).
- `skills/ideation/references/contract-template.md` — consumed by → `README.md:106` (listing), `skills/ideation/SKILL.md:308` (listing), `scripts/contract-gen.ts:1470` (**code comment** — not a listing; see Edge Cases). No skill instructions read it at runtime.
- `skills/ideation/references/agent-team-handoff.md` — zero references repo-wide (verified by grep excluding node_modules/docs) — deletion is self-contained.
- README's "Bundled references" list (:95-110) — currently omits `harness-compat.md` and `learning-filter.md` from the shared list; spec step 8.3's "confirm the tree lists" means **adding** them, plus fixing the "Shared (plugin root)" label (:97) — the shared refs live at `references/`, not the plugin root.

### Conventions (Phase 2)

- **Ownership declarations**: this repo names single owners in prose, with the mirror phrasing "The scoreboard instruction lives only here in the engine — calling skills must not duplicate it" (`interview-engine.md:160`) and execute-spec:35's "the prose owner of these semantics … Two owners, deliberately: they cross a context boundary." New owner lines (workflows/README gate table, README "One owner" section) should match this register — declare the owner, name what defers to it, state the tie-break rule.
- **Pointer style**: pointers name the owning file plus the specific section/rule, not just the file ("owned by `interview-engine.md` core rule 1", "`workflows/README.md`'s gate-behavior table"). Em-dashes and "never X" phrasing for hard rules.
- **Mantra exception**: "When unsure whether the evidence is sufficient, the gate is `not-ready`" appears in both rubric (:8) and engine (:157) deliberately — the spec explicitly keeps both copies. Do not dedupe it.
- **SKILL.md frontmatter is load-bearing**: `site/src/lib/skills.ts` parses `name`/`description`/`allowed-tools`/`disable-model-invocation` and throws on disagreement with the authored command table. None of this phase's edits touch frontmatter — keep it that way.
- **HTML/Markdown split**: HTML for interactive artifacts (contract, notes), Markdown for reference. `contract.html`/`contract.md` are generator-only output; hand-authoring either is the one forbidden move (this is why contract-template.md dies).
- **README voice**: feature sections state the rule, then the failure it prevents ("The failure it exists to prevent is a silent one…"). The new "One owner, derive or drift-test" section should follow: state the rule, list current owners (spec step 9 gives the nine entries verbatim), name the drift tests that back the mechanical ones.

### Edge Cases (Phase 2)

- **The `'Also seen in'` acceptance grep passes today.** `skills/execute-spec/SKILL.md:261` wraps the phrase across a line break ("Also seen\n  in"), so `grep -l 'Also seen in' skills/...` already returns nothing before any edit. Use `grep -n 'Also seen' skills/` as the real check — after the dedupe it should appear only in `references/learning-filter.md`.
- **`scripts/contract-gen.ts:1470` comment references the deleted file.** The markdown-builders comment block (:1465-1475) says "skills/ideation/references/contract-template.md documents it". The spec's deleted-files table claims the template is "referenced by nothing but listings" — wrong about this comment — and the acceptance grep `! grep -rn '…contract-template' scripts/ skills/ README.md` **fails** unless the builder also rewrites this comment. The File Changes table doesn't list it; treat as a required additional edit to the same file (reword to describe the legacy structure without citing the deleted doc).
- **Spec line-number drift (all targets still unique strings)**: Phase 3 opener at `ideation/SKILL.md:32` (spec: ~64); "None recorded." at `contract-gen.ts:1561` (spec: 1555); HTML render block spans :1425-1435 (spec: 1425-1431 — the template-literal ternary includes the closing `` ` '' `` lines); markdown block spans :1687-1696 (spec: 1687-1693). Cut by content, not by line range.
- **Three matrix pointers the spec doesn't name**: `autopilot:112`, `autopilot:205`, and `ideation:129` also say "execute-spec's resolution matrix". Spec step 4.3 retargets only `autopilot:76`. Leaving the others creates pointer-to-pointer chains (still resolving correctly via execute-spec's new pointer) — flag in the phase notes if left; retargeting all four to `workflows/README.md` is the cleaner read of the spec's intent.
- **"Matching shim phrasing" scope ambiguity**: harness-compat §3's table has three "(the `X` shim maps to it)" parentheticals (`Agent`, `Workflow`, `AskUserQuestion` rows). The spec's verified-unverifiable claim covers only `Agent` → `subagent`; step 5.1 says delete "the matching 'shim maps to it' phrasing" (singular). The acceptance grep checks only `'compat shim'`. Minimum: the §2 parenthetical (:32) + the `Agent` row. Whether the `Workflow`/`AskUserQuestion` shim parentheticals also die is a judgment call — they are the same species of unverified claim; the AskUserQuestion row is being edited anyway for the fallback line.
- **Cadence deletion vs gates.ts**: `## Question cadence` is an H2 before `## Gates` — deleting it cannot break the `### Gate:` parse. But the *last* gate's regex body runs to EOF, so never retitle a `### Gate:` heading while editing the rubric. Keep all five `**Gate question**`/`**Ready when**` field lines intact.
- **Qualifier preservation is the failure mode the spec calls out**: autopilot's "watched runs only" and execute-spec's "interactive runs only" headings + their unattended-skip sentences must survive the pointer reduction — flattening them re-enables capture in unattended runs, the filter's hardest rule.
- **execute-spec Completion Report rows (:224-226)**: the replacement "{one row per Validation Command in the spec}" is a template directive inside a markdown code fence — keep it inside the fence, matching the surrounding `{placeholder}` style.
- **README gate table alignment**: only the Problem Clarity cell changes ("who has it, and why?" → "who has it, and why it matters?"). The interview-engine table needs two cells (Problem Clarity + Success Criteria). The spec says "all five rows" as a rewrite instruction, but three rows already match verbatim — diff cell-by-cell instead.
- **`grep -c 'Batch up to 4' references/*.md`** post-edit expectation: 1 (interview-engine only). Today it's 2 (rubric :11 + engine :101).
- **Two deleted files, one commit**: spec rollout is a single phase commit on `ideation/pride-report`; `context-map.md` and `implementation-notes-*.html` stay out of it per execute-spec's commit conventions.

### Risks (Phase 2)

- **Spec gap, readiness-relevant but routable**: the deleted-files table's premise "contract-template.md is referenced by nothing but listings" is contradicted by `scripts/contract-gen.ts:1470` (code comment). Not a decision-log entry, and the acceptance grep forces the fix, so this does not block — but the builder must make one edit the File Changes table doesn't list. All other spec-verified claims re-verified clean: `harness-compat.md` exists as the harness-translation owner, `package.json` declares the three pi extensions, `~/.pi/agent/extensions/claude-plugin-root.ts` exists (user-level, unbundled — the honesty-edit premise holds), `agentTeamPrompt` is orphaned end-to-end (no test/fixture/site reference), `agent-team-handoff.md` has zero references, and `workflows/README.md`'s gate table already carries the scout-unavailable row.
- **Site build is the only automated guard for the rubric edit** — and it only guards gate structure, not cadence wording. A botched cadence deletion that keeps five gates but mangles surrounding prose builds green. The manual checklist item (read the edited ideation Phase 3 opener and step 7 end-to-end) is the real check for prose quality.
- **Pointer-swap circularity**: if `workflows/README.md` gains an owner line that references execute-spec's old declaration wording too literally, the two files could end up pointing at each other. Mitigation is in the spec: README keeps the full table; only execute-spec becomes a pointer. Verify by reading both after the edit.
- **`npm run build` requires site dependencies** (`pnpm -C site install` / `npm run site:install` first if `site/node_modules` is absent`). If the build cannot run in the phase environment, the rubric drift check has no fallback — treat a skipped build as a HOLD-level gap at verification time, not a pass.
- **Decision-log consistency check**: the three carried decisions (sequencing, cmd-checks-over-judgment, decision-log-retires-in-Phase-4) hold against the tree — nothing in this phase's edits implements a rejected alternative. The one factual contradiction found is the contract-template reference claim above, flagged.

---

## Phase 1 (completed)

**Gates**: 5/5 ready — Verdict: GO

| Gate                 | Status    | Evidence |
| -------------------- | --------- | -------- |
| Scope clarity        | ready     | All 7 modified files exist and each change is concrete with confirmed insertion points: `workflows/wave-planner.mjs:78` (guard after `titles` Set), `workflows/execute-contract.mjs:118` (mirror), `:694` (fixer-FAIL gate after `!fixRes.ok`), `:865-885` (planning block to wrap), `:905` (Serialized logging recompute), `:664-707` (loop to extract), `workflows/wave-planner.test.mjs` (`describe('computeWaves')` + new spawnSync block), `workflows/execute-contract.smoke.test.mjs:20` (meta regex comment), `scripts/verify.mjs:84` (null guard), `scripts/verify.test.mjs:137` (test next to 'treats an absent check as absent'), `skills/autopilot/SKILL.md:77` (one-line prereq sanity-check addition). No new or deleted files. |
| Pattern familiarity  | ready     | Every "Pattern to follow" read: unknown-prereq throw style (`wave-planner.mjs:81-90` — early throw naming offending titles), `!fixRes.ok` failure branch routing through `fail()` (`execute-contract.mjs:694-700`), `safeAgent` typed stage failures (`execute-contract.mjs:337-348`), `summarize()` return shape (`execute-contract.mjs:805-813`), symlink `isEntryPoint` test pattern (`verify.test.mjs:406-435`: mkdtemp scratch, symlinkSync, skip on no permission), tolerance philosophy comment (`verify.mjs:50-53`). |
| Dependency awareness | ready     | Blast radius mapped: `wave-planner.mjs` → `wave-planner.test.mjs` (direct import + drift suite), `execute-contract.mjs` (verbatim inline mirror), `skills/execute-spec/SKILL.md:129` (CLI invocation); `execute-contract.mjs` → smoke test + `skills/autopilot/SKILL.md` (Workflow tool); `verify.mjs:normalizeCriterion` → `scripts/contract-gen.ts:20,198` + `verify.mjs:167,322` + `verify.test.mjs:138`. |
| Edge case coverage   | ready     | Concrete list in Phase 1 Edge Cases below: duplicates-in-`phases` vs `completed` history, `plan 'null'`/`'{broken'`/`'[]'` payloads, symlinked argv[1], `normalizeCriterion(null/undefined/string/legacy/modern)`, fixer FAIL mid-cycle with `carried` fallback, typed-summary shape drift vs `summarize()`, stale-FAIL semantics through `runReviewLoop` extraction, byte-identical summaries for refactor items. |
| Test strategy        | ready     | Verified live: `node --test workflows/wave-planner.test.mjs workflows/execute-contract.smoke.test.mjs scripts/verify.test.mjs` → 100 tests, 0 fail, 1.1s. Full suite `npm test` runs `workflows/*.test.mjs scripts/*.test.mjs test-fixtures/**/*.test.mjs`. |

### Key Patterns (Phase 1)

- `workflows/wave-planner.mjs:81-90` — **unknown-prereq validation**: throws early with `new Error(\`Unknown prereq "${dep}" referenced by phase "${p.title}" — no phase has that title.\`)`. The duplicate-title guard replicates this style (throw early, name offending titles, em-dash message) immediately after the `titles` Set at line 78.
- `workflows/wave-planner.mjs:225-273` — **runCli + entry-point guard**: JSON.parse in try/catch → stderr + `process.exit(1)`; realpath-resolved argv[1] vs `import.meta.url` comparison (the symlink bug that already bit production once). New shape check goes right after the parse block, before `parsed.phases ?? []` at line ~254.
- `workflows/execute-contract.mjs:694-700` — **`!fixRes.ok` failure branch**: routes through the local `fail(summary, reviewStatus, extra)` helper with `'failed'` status, `findings`, and `reviewCycles`. The fixer-FAIL gate at step 2 follows this exactly (`if (fixRes.value.result === 'FAIL') return fail(...)` carrying the fixer's `summary`).
- `workflows/execute-contract.mjs:337-348` — **`safeAgent`**: converts agent crashes/schema-less returns into `{ ok, value, error }` typed failures "so the summary still tells the truth" — the model for the typed planner-error catch in `main()`.
- `workflows/execute-contract.mjs:805-813` — **`summarize()`**: returns `{ completed, noops, failed, skipped, results }` via `of(kind)` filters. The typed planner error must mirror this shape with empty arrays plus `error: err.message`.
- `workflows/wave-planner.test.mjs:260-313` — **`engine mirror drift` suite**: extracts `function name(...) {` to column-0 `}` from both files, strips comments/whitespace, asserts equality for `detectCycle`, `computeWaves`, `propagateSkips`, `splitWavesByFileOverlap`. Any change to `computeWaves` must be pasted identically (modulo comments/formatting) into `execute-contract.mjs:112-162` — paste, never retype.
- `workflows/execute-contract.smoke.test.mjs:15-114` — **vm smoke harness**: strips `export const meta` via regex at line 20 (the intentionally brittle line needing a comment), wraps the body in `(async function(args, agent, parallel, phase, log){...})`, injects stub agents keyed by label prefix (`scout:`, `build:`, `review:`, `fix:`, `commit:`). New smoke tests stub the `fix` label to return `{ result: 'FAIL', summary: '...' }` and feed cyclic/duplicate manifests through `run(args, stubs)`.
- `scripts/verify.test.mjs:406-435` — **symlink entry-point test**: `mkdtempSync` scratch, `symlinkSync(realDir, linkDir, 'dir')`, bare `return` (skip) on symlink permission failure. The wave-planner CLI spawnSync tests mirror this.
- `scripts/verify.mjs:50-53` — **tolerance philosophy**: "Published contracts in the wild still carry that shape, so it must keep working. Tolerant by design." The `normalizeCriterion` null guard (`if (!raw || typeof raw !== 'object') return { criterion: String(raw) };`) extends this.

### Dependencies (Phase 1)

- `workflows/wave-planner.mjs` (whole module) — consumed by → `workflows/wave-planner.test.mjs` (named imports at lines 8-14 + drift suite reads the file at :260), `workflows/execute-contract.mjs:112-216` (verbatim inline copies of 4 functions), `skills/execute-spec/SKILL.md:129` (`node ${CLAUDE_PLUGIN_ROOT}/workflows/wave-planner.mjs plan '<manifest-json>'` — live CLI surface for `--parallel` execution).
- `workflows/execute-contract.mjs` — consumed by → `workflows/execute-contract.smoke.test.mjs` (vm-compiled body), `skills/autopilot/SKILL.md` Step 3 (invoked via Workflow tool with the manifest shape documented at lines 60-72). Interface contract: returns `summarize()` shape; phases dispatched via `agent()` with labels `scout:/build:/review:/fix:/commit:`.
- `scripts/verify.mjs:84-87` (`normalizeCriterion`) — consumed by → `scripts/contract-gen.ts:20` (import), `:198` (`return normalizeCriterion(c) as NormalizedCriterion`), `scripts/verify.mjs:167` and `:322` (`(data.successCriteria ?? []).map(normalizeCriterion)`), `scripts/verify.test.mjs:19,138`. The null guard makes `normalizeCriterion(null)` return `{ criterion: 'null' }` — both downstream map sites benefit; no caller change needed.
- `skills/autopilot/SKILL.md:77` — consumed by → humans/agents following the skill; paired with the engine's new duplicate-title throw (the line tells the manifest author to check title uniqueness too).

No other external consumers found — changes are otherwise self-contained within `workflows/` and `scripts/`.

### Conventions (Phase 1)

- **Naming**: camelCase functions; SCREAMING_SNAKE constants for schemas and enums (`FIX_RESULT_SCHEMA`, `PHASE_RESULTS`); test files are `<module>.test.mjs` beside the module; smoke harness is `execute-contract.smoke.test.mjs`.
- **Imports**: ESM `.mjs`, node builtins prefixed `node:`; `execute-contract.mjs` deliberately has NO imports (Workflow sandbox may not support relative imports) — the four planner functions are inlined verbatim.
- **Error handling**: planner functions throw plain `Error` with em-dash messages naming offenders; engine `main()` currently lets them reject (this phase fixes that); stage failures go through `safeAgent` → typed `{ ok, value, error }`; phase failures go through the local `fail()` helper which prepends accumulated `warnings` to the summary.
- **Types**: JSDoc `@typedef`/`@param` only — no TypeScript in `workflows/`; `scripts/contract-gen.ts` is TS and imports verify.mjs functions. JSON Schemas (`*_RESULT_SCHEMA`) are the runtime contracts, all `additionalProperties: true`.
- **Testing**: `node --test` with `node:assert/strict`, `describe`/`it`. CLI/entry-point tests use `spawnSync` + `mkdtempSync` scratch dirs, skipping on missing symlink permission. Engine behavior is tested via the vm harness, never the real Workflow runtime. `npm test` globs `workflows/*.test.mjs`, `scripts/*.test.mjs`, `test-fixtures/**/*.test.mjs`.

### Edge Cases (Phase 1)

- Duplicate titles only checked in `phases`, never `completed` — `completed` titles are history; a resume manifest legitimately overlaps (spec failure-mode table).
- The guard's dupe-collection idiom `(seen.has(p.title) ? dupes : seen).add(p.title)` appears in the spec verbatim — use it as given so both file copies stay identical.
- CLI payloads to reject: `'null'` (parses to null), `'[]'` (array — currently silently yields `[]` waves!), `'{broken'` (already handled), `'42'`, `'"str"'`. Valid manifest must still exit 0 with waves on stdout.
- `planExecutionWaves` is exported and composed from `computeWaves` — the guard inside `computeWaves` automatically covers both the CLI and `planExecutionWaves` callers; do not duplicate it there (drift suite only mirrors the four, not the composition).
- `fixRes.value.result === 'FAIL'` access is safe only after the `!fixRes.ok` branch — a fixer omitting `result` fails schema validation → `safeAgent` catches → `ok: false` (schema has `required: ['result']`).
- Typed planner error must catch errors from BOTH `computeWaves` (line 865) and `splitWavesByFileOverlap` (line 885) — and ideally the fileless-warning block between them; wrap from line 865 through 885. The `summarize([])`-style early return at line ~856 (`phases.length === 0`) stays outside.
- Refactor items (6) must be behavior-identical: `runReviewLoop` extraction touches nothing else in `runPhase` — especially the stale-FAIL semantics (`reviewError` set mid-loop means "this FAIL stands unresolved", smoke-tested at :507-529) and the `carried` fallback to `review.findings`.
- "Serialized X after Y" provenance derivation: the splitter preserves input order and splits each prereq wave independently, so walking the already-computed `waves` in order while tracking prereq-wave boundaries reproduces the same logs — verify with the existing overlap smoke tests.
- `normalizeCriterion(null)` returns `{ criterion: 'null' }` — String(raw), matching how `normalizeCheck` stringifies `expect`.

### Risks (Phase 1)

- **Two-file change by design**: the duplicate guard in `computeWaves` must be pasted identically into `execute-contract.mjs:112-162`. The drift suite (`wave-planner.test.mjs:260`) strips comments and whitespace, so formatting may differ but code may not. Run full `npm test`, not just the scoped suite, before committing.
- **CLI behavior change for array payloads**: today `plan '[]'` exits 0 printing `[]` (arrays have no `.phases` → `?? []`). The new shape check makes it exit 1. This is intended, but it is a behavior change on a live production surface (`skills/execute-spec/SKILL.md:129`) — no test currently pins the old behavior, so nothing will catch an accidental partial revert.
- **Typed summary shape drift**: the catch-all summary must mirror `summarize()` (`execute-contract.mjs:805-813`) — if a field is later added to one and not the other, autopilot's report misreads. Construct via the same literal shape or spread where possible.
- **Extraction smuggling behavior change**: `runReviewLoop` carries the subtlest state in the file (stale-FAIL, cycle cap, carried findings). The spec demands byte-identical smoke summaries before/after; run the smoke suite between each of the three craft items, not once at the end.
- **The brittle `meta` regex is load-bearing**: `execute-contract.smoke.test.mjs:20` — the comment must say the brittleness is intentional (loud failure on shape change); do not "fix" it into a looser regex.
- **Decision-log consistency check passed**: all five "re-verified" claims held against live code — no duplicate guard existed at `wave-planner.mjs:78-91`, `fixRes.value.result` was never read (only `fixRes.value.carried` at :702-705), `plan 'null'` would TypeError at `parsed.phases` (wave-planner.mjs:255), `normalizeCriterion(null)` threw at `verify.mjs:86`, and the scoped suites ran 100/100 green. No contradictions found. (Phase 1 has since landed: the guards now exist and the suite is at 161 tests — Phase 2 must not regress them.)
